server {
	listen 8080;
	listen [::]:8080;
	server_name _;

	root /usr/share/nginx/html;
	index index.html;

	location / {
		rewrite ^/(.+)/$ /$1 break;
		try_files $uri $uri.html $uri/ /index.html;
	}
}
